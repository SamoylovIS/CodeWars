def move_zeros(lst):
    a = []
    b = []
    for i in lst:
        if i != 0:
            a.append(i)
        else:
            b.append(i)
    return a+b

#Best way
#def move_zeros(array):
#    return sorted(array, key=lambda x: x==0 and type(x) is not bool)