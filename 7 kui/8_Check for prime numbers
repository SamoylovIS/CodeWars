def is_prime(n):
    a=[]
    for i in range(1,n+1):
        if n%i==0:
            a.append(i)
    if len(a)==2:
        return True
    else:
        return False

#Best way
#def is_prime(n):
#    return n > 1 and all(n % i for i in range(2, int(n**0.5) + 1))