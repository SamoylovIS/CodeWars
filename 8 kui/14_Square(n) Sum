def square_sum(numbers):
    if len(numbers)==0:
        return 0
    else:
        sum = 0
        for i in numbers:
            sum+=i**2
        return sum

#Best way
#def square_sum(numbers):
#    return sum(x ** 2 for x in numbers)