def find_nb(m):
    sum = m
    count = 0
    while sum>0:
        sum = sum - (count+1)**3
        count+=1
        if sum == 0:
            return(count)
        if sum < 0:
            return(-1)

#Best solution
#def find_nb(m):
#    n = 1
#    volume = 0
#    while volume < m:
#        volume += n**3
#        if volume == m:
#            return n
#        n += 1
#    return -1